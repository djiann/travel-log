# 東京行程指南｜打包轉移與 GitHub Pages 部署教學

把整個 `Travel` 資料夾打包帶到**個人電腦**，依本文件操作即可取得 HTTPS 連結，同伴用 Line 點開會在 Safari 正常顯示（含折疊區塊）。

---

## 一、打包帶走哪些檔案？

### ✅ 必帶

```
Travel/
├── docs/                    ← 已建好的網站（可帶；到新電腦也可重建）
├── assets/                  ← 地圖 PNG（必帶）
├── build-site.py            ← 一鍵重建網站（含三檔同步）
├── build-guides.py          ← 同步暗色版 + 手機單檔版
├── build-dark-guide.py      ← light → dark
├── build-mobile-guide.py    ← light → mobile
├── deploy-github-pages.sh   ← 部署輔助腳本
├── .gitignore
├── tokyo-2026-guide-light.html
├── tokyo-2026-guide.html
├── journey-maps.html
└── 部署教學.md              ← 本文件
```

### ⛔ 可不帶（體積大、可重建）

| 檔案/資料夾 | 原因 |
|-------------|------|
| `Generated_image_*.png`（根目錄大圖） | 原始 AI 圖，已匯入 `assets/` |
| `.mobile-build/` | 建置暫存 |
| `.cursor/` | 編輯器設定 |
| `.DS_Store` | macOS 系統檔 |
| `tokyo-2026-guide-mobile.html` | 執行 `python3 build-guides.py` 可重建 |
| `tokyo-2026-guide.html` | 執行 `python3 build-guides.py` 可重建（勿手改內容） |
| `journey-map-data.js` / `.json` | 舊版 SVG 用地圖資料，現已不用 |

### 打包方式建議

- **zip**：右鍵 `Travel` 資料夾 → 壓縮
- 或 **AirDrop / 隨身碟 / 個人雲端**（Google Drive 個人帳號、iCloud 等）

---

## 二、個人電腦事前準備

1. 安裝 **Git**：https://git-scm.com/downloads  
2. 瀏覽器登入**個人** GitHub（建議無痕或個人瀏覽器設定檔，避免混到公司帳號）  
3. 確認已安裝 **Python 3**（macOS 通常內建；Windows 從 python.org 安裝）

終端機檢查：

```bash
git --version
python3 --version
```

---

## 三、在本機重建網站（建議做一次）

解壓縮後進入資料夾：

```bash
cd Travel    # 或你的實際路徑

python3 build-site.py
```

成功會看到：

```
Site ready: .../docs/
  index.html  ← 分享這個網址（同伴主入口）
  maps.html   ← 插畫地圖分頁
  guide.html  ← 完整指南（清新版）
```

### 本機預覽（選用）

```bash
cd docs
python3 -m http.server 8080
```

瀏覽器開 http://localhost:8080/ ，手機同一 Wi‑Fi 可開 `http://電腦IP:8080/`

---

## 四、建立個人 GitHub 倉庫

1. 登入 **個人** https://github.com  
2. 右上角 **+** → **New repository**  
3. 設定：
   - Repository name：`tokyo-2026-trip`（可自訂）
   - **Public**
   - 不要勾選 Add a README
4. 按 **Create repository**

記下你的帳號，之後網址會是：

```
https://你的帳號.github.io/tokyo-2026-trip/
```

---

## 五、用個人身份初始化 Git（勿改全域設定）

在 `Travel` 資料夾內：

```bash
cd Travel

git init
git branch -M main

# ⚠️ 只設定這個專案，不要加 --global
git config user.name "你的名字"
git config user.email "你的個人信箱@example.com"

git add docs/ build-site.py build-mobile-guide.py deploy-github-pages.sh .gitignore
git add tokyo-2026-guide-light.html tokyo-2026-guide.html journey-maps.html assets/
git add 部署教學.md

git commit -m "Deploy Tokyo trip guide to GitHub Pages"
```

---

## 六、推送到個人 GitHub

本專案已設定為使用 **SSH 方式** 連線，這是最方便且後續不需反覆輸入密碼的作法。

### SSH 設定方式（已完成）

1. 產生 SSH 金鑰：
   ```bash
   ssh-keygen -t ed25519 -C "你的個人信箱" -f ~/.ssh/id_ed25519_personal
   ```
2. 將 `~/.ssh/id_ed25519_personal.pub` 的內容複製，貼到 GitHub → **Settings → SSH and GPG keys → New SSH key**。
3. 在 `~/.ssh/config` 中設定別名（避免與公司帳號衝突）：
   ```text
   Host github.com-personal
     HostName github.com
     User git
     IdentityFile ~/.ssh/id_ed25519_personal
   ```
4. 設定 Remote 並推送：
   ```bash
   git remote add origin git@github.com-personal:你的帳號/tokyo-2026-trip.git
   git push -u origin main
   ```
*(註：原本的 HTTPS + Personal Access Token 方式容易遇到 Token 過期或輸入錯誤的問題，建議全面改用 SSH。)*

---

## 七、開啟 GitHub Pages

1. 打開 repo 頁面  
2. **Settings** → 左側 **Pages**  
3. **Build and deployment**：
   - Source：**Deploy from a branch**
   - Branch：**main**
   - Folder：**/docs**
4. **Save**

等 **1～2 分鐘**，重新整理 Pages 頁面，會出現：

```
https://你的帳號.github.io/tokyo-2026-trip/
```

### 分享給同伴

| 連結 | 用途 |
|------|------|
| `https://你的帳號.github.io/tokyo-2026-trip/` | **主入口**（手機版，折疊+地圖） |
| `.../maps.html` | 插畫地圖分頁 |
| `.../guide.html` | 完整清新版指南 |

Line 傳主連結即可；點開會用 Safari / 預設瀏覽器開啟，折疊可正常點擊。  
建議同伴：**分享 → 加入主畫面**，出發後也能快開。

---

## 八、之後修改行程與推送到 GitHub

當您修改了行程內容或地圖後，請依照以下步驟更新網站：

1. **只**在本機編輯 `tokyo-2026-guide-light.html`（行程唯一來源）。
2. 同步三檔並重建 `docs/`：
   ```bash
   cd ~/VS_workspace/Travel
   python3 build-guides.py    # 產生 dark + mobile（改行程必跑）
   python3 build-site.py      # 若要更新 GitHub Pages，再跑這個
   ```
3. 將變更推送到 GitHub（見下方 **方式 A（GUI，推薦）** 或 **方式 B（指令列）**）。

約 1-2 分鐘後，同伴重新整理網頁即為新版。

> **⚠️ 最常見的誤會：push 了但手機還是舊版**
>
> GitHub Pages **只讀 `docs/` 資料夾**，不會直接讀根目錄的 HTML 或 `assets/`。
> 只改根目錄並 push，**線上網站不會變**；必須先執行 `python3 build-site.py` 重建 `docs/`，再把 **`docs/` 一起 commit + push**。
>
> | 你本機編輯的 | GitHub Pages 實際顯示的 |
> |-------------|------------------------|
> | `assets/*.png`（根目錄） | `docs/assets/*.png` |
> | `tokyo-2026-guide-light.html` | `docs/index.html`（首頁）或 `docs/guide.html` |
> | `journey-maps.html` | `docs/maps.html` |
>
> **正確網址**（請傳這個給同伴）：
> `https://你的帳號.github.io/tokyo-2026-trip/`
>
> 在 GitHub App 裡點 repo 內的 `.html` 檔預覽，**不是**正式網站。push 後若仍看到舊版，請用 Safari 開正式網址並**下拉重新整理**（或清除快取）。

---

### 方式 A：用 GUI 推送（推薦，不用打指令）

若覺得 `git add` / `git commit` / `git push` 太繁瑣，可用圖形介面完成同一件事。

#### A-1. Cursor / VS Code 內建 Git（最方便）

你現在就在 Cursor 裡，最省事：

1. 左側點 **Source Control**（分支圖示，快捷鍵 `Ctrl+Shift+G` / macOS `Cmd+Shift+G`）
2. 在 **Changes** 清單中勾選要提交的檔案，例如：
   - **`docs/`（必勾！執行 `build-site.py` 後才會出現變更）**
   - `assets/*.png`（地圖圖片，根目錄來源檔）
   - `tokyo-2026-guide-light.html`、`tokyo-2026-guide.html`
   - `travel-planning-template.md`、`部署教學.md` 等
3. 上方輸入 commit 訊息，例如：`更新行程地圖與指南`
4. 點 **Commit**（或 ✓ 提交）
5. 再點 **Sync Changes** 或 **Push**（向上箭頭圖示）

等同於終端機的 `git add` → `git commit` → `git push`，全程用滑鼠即可。

#### A-2. GitHub Desktop

適合不想碰終端機、偏好獨立 App 的使用者：

1. 下載安裝：https://desktop.github.com/
2. **File → Add Local Repository**，選擇 `Travel` 資料夾
3. 左側會列出變更的檔案
4. 左下角填 **Summary**（commit 訊息），例如：`更新行程地圖`
5. 按 **Commit to main**
6. 按右上角 **Push origin**

#### A-3. GitLens（擴充功能）

若已安裝 GitLens，也可在左側 **Source Control** 檢視變更、右鍵 stage 檔案、提交後用同步按鈕 push，操作與 Cursor 內建 Git 類似。

#### 其他 GUI 選項（選用）

| 工具 | 說明 |
|------|------|
| **Fork** | macOS 上常見的 Git 圖形客戶端 |
| **Sourcetree** | 免費，功能完整 |
| **GitHub 網頁** | 只能瀏覽 repo，**無法**從本機直接 push；上傳檔案請見第九節 Plan B |

---

### 方式 B：指令列推送

```bash
git add docs/ assets/ tokyo-2026-guide-light.html  # docs/ 必加，否則線上不會更新
git commit -m "更新行程內容"
git push
```

若一次更新很多檔案，也可：

```bash
git add .
git commit -m "更新行程地圖與指南"
git push origin main
```

**⚠️ 關於 `cursoragent` 共同作者問題：**
如果您使用 Cursor 內建終端機或介面執行 commit，Cursor 預設可能會在 commit 訊息中加入 `Co-authored-by: Cursor <cursoragent@cursor.com>`。
- **解決方法 1（推薦）**：到 **Cursor Settings → Agents → Attribution**，關閉 **Commit Attribution** 與 **PR Attribution**，然後重啟 Cursor。
- **解決方法 2**：使用 macOS 內建的 **Terminal.app** 來執行 `git commit` 與 `git push`。
- **事後補救**：如果不小心加到了，可以在 Terminal.app 執行 `git commit --amend` 刪除該行，再用 `git push --force-with-lease origin main` 強制覆蓋。

**🔑 關於 GitHub 登入與 SSH：**
本專案已設定使用 SSH 金鑰連線（即 `git@github.com-personal:djiann/tokyo-2026-trip.git`）。
- 您**不需要**再輸入 GitHub 密碼或 Personal Access Token (PAT)。
- 只要本機的 `~/.ssh/id_ed25519_personal` 金鑰已加入 GitHub，直接 push（GUI 或指令列）即可成功。

若也要更新手機單檔 HTML（非網頁版）：

```bash
python3 build-mobile-guide.py
# 產生 tokyo-2026-guide-mobile.html，可另傳 Line
```

---

## 九、沒有 Git 或公司網路擋 push？

### 網頁上傳（Plan B）

1. 個人 GitHub 建好空 repo  
2. **Add file → Upload files**  
3. 把 `docs/` 裡**所有檔案**拖進去（含 `assets` 子資料夾）  
4. Commit  
5. Settings → Pages → Branch: **main**，Folder: **/ (root)**（因為檔案在 root 而非 docs 子目錄）

之後每次更新需重新上傳；有 Git 會方便很多。

---

## 十、常見問題

### Q：push 了但手機還是舊版？

1. 確認有執行 **`python3 build-site.py`**，且 **`docs/` 已一起 push**（見第八節對照表）。
2. 確認開的是 **GitHub Pages 網址**（`https://帳號.github.io/tokyo-2026-trip/`），不是在 GitHub App 裡預覽 repo 內的 `.html` 檔。
3. push 後等 **1～2 分鐘**，手機 Safari **下拉重新整理**（必要時清除該頁快取）。
4. 到 GitHub repo 網頁確認 `docs/assets/` 裡的 PNG 檔案日期與大小是否已更新。

### Q：同伴折疊還是打不開？

- 確認傳的是 **https 網址**，不是本機 `.html` 檔  
- 請用 Safari 開，並「加入主畫面」

### Q：圖片破圖？

- 執行 `python3 build-site.py` 確認 `docs/assets/` 有 8 張 PNG  
- push 後確認 GitHub 上 `docs/assets/` 存在

### Q：Pages 顯示 404？

- 確認 Pages 設定為 **main** + **/docs**  
- `docs/index.html` 必須存在  
- 等 2～5 分鐘再試

### Q：想設成私密，只給家人？

- 個人免費帳號：**Private repo 也可開 Pages**（2020 年起支援）  
- 建 repo 時選 **Private**，Pages 設定相同；網址知道的人仍可開啟（無 GitHub 登入牆），若需密碼保護要用 Netlify 等進階方案

### Q：公司電腦 vs 個人電腦？

- 本教學假設在**個人電腦**操作，用**個人 GitHub**  
- 勿把 repo 建在公司 organization 下  
- 此專案 `git config` 請只用**本資料夾**設定，不要用 `--global` 覆蓋公司設定

---

## 十一、檔案說明速查

| 檔案 | 說明 |
|------|------|
| `tokyo-2026-guide-light.html` | **唯一手改**的行程來源（清新版） |
| `tokyo-2026-guide.html` | 深色版（`build-guides.py` 自動產生） |
| `tokyo-2026-guide-mobile.html` | 單檔離線版（`build-guides.py` 自動產生） |
| `journey-maps.html` | 地圖分頁檢視器 |
| `docs/index.html` | **GitHub Pages 首頁** |
| `build-guides.py` | light → dark + mobile |
| `build-site.py` | 重建 `docs/`（內含 `build-guides.py`） |

---

## 十二、最短流程（懶人包）

```bash
cd Travel
python3 build-guides.py   # 改行程後必跑
python3 build-site.py     # 部署前重建 docs/
git init && git branch -M main
git config user.email "個人信箱"
git config user.name "你的名字"
git add docs/ assets/ *.html build-*.py deploy-github-pages.sh .gitignore 部署教學.md
git commit -m "Deploy Tokyo trip guide"
git remote add origin https://github.com/你的帳號/tokyo-2026-trip.git
git push -u origin main
```

→ GitHub **Settings → Pages → main /docs**  
→ 分享 `https://你的帳號.github.io/tokyo-2026-trip/`

祝旅途愉快 🗼
